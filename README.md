# 勤怠管理システム(kintai)

株式会社伊豆倉組の勤怠管理システム。タッチオンタイム・タイムツリー・シャチハタクラウドの3つを、このシステム1つに統合するのが目的。9/1リリース目標。Asanaプロジェクト:https://app.asana.com/1/1216844938289140/project/1217137088641946

## 構成

- **フロント**:素のHTML/CSS/JavaScript(フレームワークなし)。GitHubにpush→Vercelが自動デプロイ。
- **バックエンド**:Supabase(PostgreSQL + Auth + Edge Functions)。サーバー(Railway等)は使わない方針。
- **メール送信**:Resend(送信元 `kintai-noreply@izukura.co.jp`)。Edge Functions経由でのみ使用(ブラウザに鍵を置かないため)。
- **祝日データ**:`holidays-jp.github.io`の無料APIから取得。
- **本番URL**:`kintai-ten-ruby.vercel.app`
- **GitHubリポジトリ**:`t09066982849-tech/kintai`

## ファイル一覧と役割

| ファイル | 役割 |
|---|---|
| `common.js` | 全画面共通。Supabaseクライアント初期化、login/logout、requireEmployee(ログイン確認+従業員情報取得+管理者ナビ表示)、ensureSession |
| `index.html` / `app.js` | 打刻画面。出退勤、GPS、現場選択、月次履歴、修正申請、現在時刻表示、みなし残業集計 |
| `schedule.html` / `schedule.js` | スケジュール(カレンダー)画面。予定登録・編集・削除、複数日対応、参加メンバー、祝日表示 |
| `leave.html` / `leave.js` | 有給・出張申請画面。3段階承認(部長→常務→社長)、出張旅費自動計算、取り消し機能 |
| `admin.html` / `admin.js` | 管理者専用画面。打刻修正の承認、現場マスタ管理、Excel出力(従業員別シート)、承認済み申請書類の閲覧・削除 |
| `document.html` / `document.js` | 有給休暇届・出張申請書のPDF書類(印影入り、承認完了後のみ表示、印刷/PDF保存ボタン) |
| `style.css` | 全画面共通スタイル |

## データベース(Supabase)テーブル

- `employees`:従業員マスタ。`is_admin`、`department`(civil/accounting)、`role`(president/director/manager_civil/manager_accounting/null)、`travel_rate_group_id`、`paid_leave_balance`(残り有給日数、現在全員0で初期化済み・実データ反映待ち)
- `sites`:現場マスタ。`work_start`/`work_end`(所定時間、事務所は8:30-17:30、その他は7:00-17:00がデフォルト。残業・補正計算に使用)
- `time_records`:打刻記録。実時刻のみ保存(計算型)。出退勤GPS付き。**実時刻は画面・Excel・管理者にも一切表示しない**(証跡として裏に保持するのみ)。表示・出力は必ず`common.js`の`computeDayMetrics`で補正した値を使う
- `correction_requests`:既存の打刻記録の時刻+現場を直す申請。管理者が承認/却下。出退勤どちらも入力必須(2026-08-19、片方のみでの申請を禁止)。**みなし残業制のため、早出・残業を含む時刻もそのまま申請できる(自動丸めは行わない、2026-08-19に撤廃)**。却下された場合は「却下」表示の横に再申請ボタンが出る
- `missing_record_requests`:打刻記録が丸ごと存在しない日の追加申請(出勤忘れ・退勤忘れの当日も含む)。`requested_clock_in`必須、`requested_clock_out`は任意。承認されると新規`time_records`行が作られる(退勤未入力ならNULLのまま、後で通常の退勤ボタンが使える)。**こちらも早出・残業の自動丸めは撤廃済み**。却下時は再申請ボタンが出る
- `schedules`:スケジュール。`type`は event/paid_leave/business_trip。複数日対応(`end_date`)。全員閲覧可
- `schedule_members`:スケジュールの参加メンバー(多対多)
- `leave_requests`:有給・出張申請。3段階承認の状態を保持。出張は`zone`/`daily_allowance`/`hotel_fee`/`total_amount`も持つ。有給は全承認完了時に`employees.paid_leave_balance`から自動減算
- `travel_rate_groups` / `travel_rates`:旅費規程(グループ単位で道内/道外の日当・宿泊費)
- `holidays`:全国の祝日マスタ(`sync-holidays`で自動取得)。**経理部の休日判定にのみ使用**。土木部は使わない(下記参照)
- `company_holidays`:会社独自の休業期間(GW・お盆・年末年始など、`start_date`〜`end_date`)。**土木部の休日判定に使用**。現在登録済み:GW(5/3-5/5)、お盆(8/13-8/15)、年末年始(12/29-1/4)。年ごとに手動で追加・更新が必要

### 部署によって休日の考え方が違う(重要)

土木部は現場都合で祝日(飛び石)でも稼働することが多いが、GW・お盆・正月はまとまって休業する。経理部は祝日を含めて通常のカレンダー通り休む。そのため打刻漏れチェック(`check-missing-punches`系)は部署ごとに判定ロジックを分けている。

- 経理部(`department = 'accounting'`):土日 + `holidays`テーブルの祝日を除外
- 土木部(それ以外):土日 + `company_holidays`の期間のみ除外(祝日そのものは通常営業として扱う)

スケジュールのカレンダー表示(`schedule.js`)は全社共通で`holidays`を赤字表示に使っているが、これは表示上の参考情報であり、打刻漏れ判定とは連動していない。

## Supabase Edge Functions

- `check-missing-punches`:毎日12時実行(cron設定済み)。実行日自体が土日なら即終了(2026-08-17追加)。それ以外は「前日」ではなく、部署ごとの休日ルールに従って遡った直近の稼働日分の打刻漏れを検知しメール通知(2026-08-12修正:部署別の休日判定に対応)。**現在RESTRICT_TO_SHIMAKI=trueで嶋木さんのみ対象**(全員分の動作確認が済んだらfalseに戻す)
- `check-missing-punches-biweekly`:毎月1日・16日12時実行(cron設定済み)。直近15日分をまとめて棚卸し。部署別の休日ルールに対応済み(2026-08-12)。**まだ絞り込み(RESTRICT_TO_SHIMAKI相当)が入っていないため、実行すると28人全員分がTEST_MODE宛先(嶋木さん)に届く**。8/16の本番実行結果を見てから絞り込み追加を判断する
- `notify-leave-approval`:leave.jsから都度呼び出し。申請時→部長、各承認時→次の承認者、全承認完了時→申請者本人にメール
- `sync-holidays`:祝日データ取得(手動実行。今後は年1回の自動実行cronを検討)
- `create-employee`(2026-08-下旬追加):admin.jsから呼び出し。管理者のみ実行可(呼び出し元がis_adminか検証)。ログインアカウント作成(`auth.admin.createUser`)+`employees`への登録をまとめて1回で行う。ログインアカウントは作れてもemployees登録に失敗した場合は作成したアカウントを削除してロールバックする

**3つの通知系Edge FunctionsはすべてTEST_MODE=trueのまま。本番切り替え時は`TEST_MODE = false`に変更し、`RESTRICT_TO_SHIMAKI`も外すこと。**

## 承認者(role)

- 社長:伊豆倉 寿信(president)
- 常務:伊豆倉 米郎(director)
- 土木部長:山本 英嗣(manager_civil)
- 経理部長:佐藤 秀樹(manager_accounting)
- 経理部所属:松浦 春那・佐藤 秀樹・山崎 太一・平野 聖美・古澤 直理・嶋木 正、それ以外は土木部

役職交代時は`employees.role`をSQLで書き換えるだけでよい(コード修正不要)。

## 旅費規程グループ

- 社長(伊豆倉寿信):道内 日当6000/宿泊12000、道外 日当9500/宿泊14000
- 取締役(伊豆倉米郎・伊豆倉鈴雄・佐々木紀康・大坂朝夫):道内 日当5000/宿泊9000、道外 日当8000/宿泊11000
- 大角賢一:道内 日当3000/宿泊8000、道外なし
- 基本(上記以外全員):道内 日当3000/宿泊7000、道外 日当5000/宿泊9000

宿泊費は「日数-1」を宿泊数として計算。

## できていること(9/1リリースに向けて概ね完成)

- 打刻(GPS・現場選択・履歴・月切り替え・現在時刻表示・セッション切れ検知・連打防止)
- 打刻修正申請(既存記録の時刻+現場、早出・残業は所定時刻に自動修正、承認/却下)
- 打刻記録の追加申請(出勤自体を忘れた日向け。出退勤どちらも入力可、退勤は空欄可、当日分も対応)
- スケジュール(カレンダー・複数日・終日/時刻指定・参加メンバー・祝日表示・表示優先順位)
- 有給・出張申請(3段階承認・自動スキップ・取り消し・出張旅費自動計算・承認後スケジュール自動反映)
- 申請書類のPDF化(印影入り、有給/出張で別レイアウト)
- 管理者機能(現場管理・Excel出力・承認済み書類の閲覧削除・打刻記録追加申請の承認)
- 打刻漏れ通知(毎日+半月、部署別の休日ルールに対応、cron設定済み)
- 承認フェーズ通知(申請〜完了まで一通り)
- 4画面の相互ナビゲーション(管理者のみ「管理」リンク表示)
- 28名分の本番アカウント登録済み
- パスワード変更機能(勤怠画面内)
- 出退勤時刻・勤務時間・残業時間の補正表示(出退勤それぞれ最大1時間まで早出・残業を認定、現場ごとの所定時間`sites.work_start/work_end`を反映。**実時刻はDBに残るのみで画面・Excelには一切表示しない**。計算ロジックは`common.js`の`computeDayMetrics`に一本化、app.js/admin.jsで共用)
- 残業時間の月次表示・Excel出力(赤文字で強調表示、Excelは補正後の出退勤時刻+勤務時間+残業時間の列。GPS位置情報の列は削除)
- 有給の自動減算(承認完了時に`employees.paid_leave_balance`から差し引き)、残り有給日数の画面表示(赤文字)
- 管理者画面から新規従業員を登録できる機能(氏名・メール・パスワード・部署を入力するだけで、ログインアカウント作成+employees登録がまとめて完了する)

## 残タスク

- [x] 8/16、`check-missing-punches-biweekly`の部署別休日ロジックの本番実行確認 → 正常動作確認済み(未絞り込みのため28人全員分が嶋木さん宛に届いた)
- [ ] 次の土日、`check-missing-punches`が実行スキップされるか確認(2026-08-17の修正効果)
- [ ] `check-missing-punches-biweekly`にRESTRICT_TO_SHIMAKI相当の絞り込みを追加するか判断(現状28人全員分がTEST_MODE宛先に届く)
- [ ] 全28名分の本番アカウントでの動作確認
- [ ] スマホでの表示確認(特にスケジュールのカレンダー)
- [ ] 3つの通知系Edge FunctionsのTEST_MODE解除(リリース直前)
- [ ] 操作説明書の作成
- [ ] システム紹介プレゼン資料の作成
- [ ] 9/1本番リリース

## 管理者アカウントのパスワードを忘れた場合

Supabaseの管理画面(Authentication → Users)には、管理者からパスワードを直接書き換えるボタンがない(メール送信によるリセットのみ)。`admin@izukura.co.jp`は実際に受信できるメールアドレスではないため、この方法は使えない。代わりにSQL Editorで直接更新する。

```sql
update auth.users
set encrypted_password = crypt('新しいパスワード', gen_salt('bf'))
where email = 'admin@izukura.co.jp';
```

`pgcrypto`拡張が必要な場合はエラーが出るので、その場合は先に有効化すること。

## 実装しない方針(2026-08-10確定)

- プッシュ通知(スケジュールのリマインダー) → 実装しない。通知はメール(承認フェーズ・打刻漏れ)のみで運用する
- 有給・出張申請書のFileforce自動保存 → 実装しない。管理者がadmin.htmlの「承認済み申請書類」から手動でPDF保存し、Fileforceに手動アップロードする運用のままとする

## 残タスク(未実装・データ整備待ち)

- 有給の実残日数を各従業員分調べて`employees.paid_leave_balance`に反映する(現在は全員0日)
- `company_holidays`は年ごとに手動更新が必要(2027年以降のGW・お盆・年末年始は都度登録すること)

## 有給の自動付与ロジック(検討中・未実装)

会社の共有サーバーで管理されているExcel(`個人別年次有給休暇管理簿`)を確認したところ、以下の情報が管理されていた。

- 入社日、基準日(付与日、入社半年後など)、有効期間
- 前年度繰越日数、今年度付与日数、合計日数、次年度繰越
- 月ごとの取得日数・残日数、日付ごとの取得記録(1日〜31日のマス目)

### 方針(2026-08-下旬に整理・合意済み)

- 経理に「日付ごとの詳細な取得記録は本当に必要か」確認したところ、明確な理由は得られなかった。労働基準法上必要なのは「残数」「取得日」「取得日数」の3点のみで、Excelのような月別カレンダー形式の一覧表自体は必須ではない。
- 「取得日」の記録は、実は`leave_requests`テーブル(承認された有給申請、開始日・終了日・日数を保持)がそのまま証跡になるため、**新たに詳細な取得履歴テーブルを作る必要はない**。
- 対応として決まっているのは以下の2点:
  1. `leave_requests`から`type='paid_leave' and status='approved'`を抽出し、Excel出力できるボタンをadmin.jsに追加する(取得日一覧の代替。**未実装**)
  2. 基準日・繰越・自動付与のロジックを新規に作る(**未実装、詳細未確定**)

### 次に決めるべきこと(次のチャットで再開する場合はここから)

- 基準日・自動付与のルールを、一般的な労働基準法の基準(入社から6ヶ月後に10日付与、以降1年ごとに勤続年数に応じて追加付与)で進めてよいか、それとも会社独自のルールがあるか、を確認する必要がある
- 繰越日数の上限(法律上は2年で時効消滅)をどう扱うか
- 上記が決まったら、`employees`に`hire_date`(入社日)・`base_date`(基準日)などの列を追加し、付与処理を行うEdge Function(または管理者が手動実行するボタン)を新設する想定

- 出張申請の交通費・宿泊費のFileforce証跡運用ルール確定(上記の方針により手動運用前提で確定させる)

## 開発ルール

- **合言葉方式**:コード変更は必ず内容を先に説明し、ユーザーが「やっちゃいな」と言うまで実行しない(GitHubコミット・Supabase操作とも)。「進めよう」等の曖昧な同意では実行しないこと。これまで複数回、この合言葉ルールを守らず実装まで進めてしまった事例があった(2026-08-06、2026-08-11)。「次にやることを説明した直後、その説明自体を許可と混同する」のが典型的な失敗パターンなので、コードの説明をした回では絶対にツールを呼ばず、必ず一度返答を止めて次のユーザー発言を待つこと。
- GitHubへの直接コミットは、Claudeが専用のFine-grained PAT(このリポジトリのみ、Contents:Read and write)を使って行っている。トークンは別途ユーザーから都度受け取る運用。
- Supabase(SQL実行、Edge Functions編集、Secrets登録)はユーザーが手動で行う(直接接続は未設定)。
- 複数アカウントでの同時ログインテストは、タブではなく別ブラウザ/シークレットウィンドウを使うこと(セッションが競合するため)。

## 修正履歴(重要なバグ)

- **2026-08-10**:`check-missing-punches`で2つのバグを発見・修正。①「前日」を単純計算していたため、月曜実行時に日曜(対象外の日)を見てしまい、金曜分の打刻漏れを見逃していた→土日・祝日を遡って直近の平日を探す方式に修正。②スケジュール除外判定で、`end_date`がnull(単日の予定)の場合を「無期限」と誤解釈し、1日だけの有給がそれ以降ずっと除外対象になっていた→`end_date`がnullの場合は`date`と同日のみ対象とするよう修正。`check-missing-punches-biweekly`は元々正しい書き方だったため同バグはなし。
- **2026-08-11**:`common.js`の`computeDayMetrics`で、`sites.work_start/work_end`(DB上はtime型で秒付き文字列`08:30:00`)を分付き前提(`08:30`)で日時組み立てしていたため、`Invalid Date`(NaN)が発生し残業時間・補正表示が壊れていた。→ `.slice(0, 5)`で先頭5文字に切り詰めてから使うよう修正。
- **2026-08-12**:祝日一律除外の設計不備が判明。当初「祝日は全員休み」という前提で`check-missing-punches`系を実装したが、実際は土木部が祝日(飛び石)でも稼働することがあり、逆に経理部は祝日通り休む。単純な「連休なら休み」ルールも実態(月曜祝日でも稼働日がある)に合わず断念。→ 部署ごとに休日判定ロジックを分岐(経理部=`holidays`、土木部=`company_holidays`のみ)する方式に変更。
- **2026-08-17**:`check-missing-punches`が、実行日自体が土日でも動き続け、直近の平日(金曜)分の打刻漏れがまだ直っていないと毎日重複して通知される不具合を確認(8/15・8/16に8/14分の通知が重複)。→ 関数の先頭で「実行日自体が土日ならスキップして終了」する処理を追加。次の土日で効果を確認予定。
- **2026-08-19**:複数の不具合・仕様見直しをまとめて対応。①`time_records`テーブルに管理者からのINSERTを許可するRLSポリシーが無く、`missing_record_requests`の承認処理が全て失敗していた→ `time_records_insert_admin`ポリシーを追加。②`admin.js`の`loadMissingRequests`が、`employees`への複数外部キー(申請者用・承認者用)で参照先を特定できずエラーになっていた→ `employees!missing_record_requests_employee_id_fkey`と外部キー名を明示して修正(過去にも`schedules`や`leave_requests`で同種のバグが発生している。**新しいテーブルで`employees`を複数回参照する設計にする際は、必ず外部キー名を明示すること**)。③管理者画面のセクション順を「Excel出力→修正申請→追加申請→承認済み書類→現場管理」に並べ替え(分かりやすさ重視)。④修正申請で出勤・退勤どちらも未入力のまま送信できてしまう不具合を修正、両方必須に。⑤修正申請・追加申請が却下された場合、再申請できず詰んでいた不具合を修正、却下表示の横に再申請ボタンを追加。⑥**方針転換:みなし残業制のため、早出・残業を所定時刻に自動で丸める処理(2026-08-19以前の実装)を撤廃**。自己申告のまま(位置情報の裏付けがなくても)認める方針に変更。理由:みなし残業のため多く申告されても追加の支払いは発生せず、丸める実利がない一方、実態を隠さない方が労務管理上健全なため。

## 秘密情報の所在(値はここに書かない)

- Supabase Publishable Key:各HTML内の`supabaseUrl`/`supabaseKey`に直書き(公開情報のため問題なし)
- Resend APIキー:Supabase Edge Functions の Secrets(`RESEND_API_KEY`)
- Supabase service_role キー:Supabase Vault(`service_role_key`という名前で保管、cronから参照)
- GitHub PAT:ユーザーが管理。Claudeとの会話内で都度共有
