# Claude Codeへの引き継ぎ事項(kintaiプロジェクト)

このファイルはClaude Code用の指示ファイル。プロジェクトの詳しい状況(構成・データベース・できていること・残タスク)は`README.md`を必ず先に読むこと。ここには**開発ルール**と**環境の制約**だけをまとめる。

## 最初にやること

作業を始める前に、必ず`README.md`を全文読むこと。特に「残タスク」「修正履歴」「有給の自動付与ロジック(検討中)」のセクションは、今どこまで進んでいて何が未確定かを正確に把握するために重要。

## 開発ルール(厳守)

### 合言葉方式

コードの変更(ファイル編集、コミット、Supabaseへの操作)は、必ず**内容を先にユーザーに説明し、ユーザーが「やっちゃいな」と言うまで実行しないこと**。

- 「進めよう」「いいよ」「そうしよう」のような曖昧な同意では実行しない。
- 自分が「実装します」「直します」と宣言した直後、その宣言自体を許可だと勝手に解釈して実行に進んではいけない。これは過去に何度も起きた失敗パターン(2026-08-06、2026-08-11、2026-08-19など、README.mdの修正履歴に詳細あり)。
- コードの説明をした回では、絶対にファイル編集やコミットのツールを呼ばず、必ず一度応答を止めて、次のユーザーの発言を待つこと。
- 「やっちゃいな」という文字列が明確に含まれているときだけ実行する。「実質同じ」という自己判断で拡大解釈しないこと。

Claude Code環境ではファイル編集やコマンド実行がより直接的にできてしまう分、この合言葉ルールを守ることがなお一層重要になる。

### Supabaseの操作範囲

- SQL実行、Edge Functionsのコード編集・デプロイ、Secretsの登録は、ユーザーが手動でSupabaseの管理画面から行う運用になっている(Claude側からの直接接続は未設定)。
- Claude Code環境でSupabase CLIやAPIキーを使った直接操作ができる状態になっていたとしても、上記の合言葉ルールは同様に適用される。実行前に必ず内容を説明し、「やっちゃいな」を待つこと。

### GitHubの操作

- 通常のClaude.aiチャットでは、ユーザーから都度Fine-grained PAT(このリポジトリのみ、Contents:Read and write)を受け取ってコミットしていた。
- Claude Code環境では、ローカルのgit操作(`git add`, `git commit`, `git push`など)がそのまま使えるはずなので、そちらを使ってよい。ただし実行前の合言葉ルールは変わらない。

### その他の運用上の注意

- 複数アカウントでの同時ログインテストは、タブではなく別ブラウザ/シークレットウィンドウを使うこと(Supabaseのセッションが競合するため)。
- 新しいテーブルで`employees`テーブルを複数回参照する設計にする場合、必ずSupabaseクエリで外部キー名を明示すること(`employees!テーブル名_列名_fkey(...)`のように)。過去に`schedules`、`leave_requests`、`missing_record_requests`で、参照先を特定できず失敗するバグが繰り返し発生している。
- DBのtime型の列(`work_start`など)は秒付き文字列(`08:30:00`)で返ってくるため、JavaScript側で時刻を組み立てる際は`.slice(0, 5)`で分までに切り詰めてから使うこと。過去にこれを忘れてNaN(Invalid Date)になるバグが発生している。

## ユーザーについて

嶋木正さん。株式会社伊豆倉組でIT改善・DX推進を担当。技術的な前提知識は少ないが、業務ロジック・労務の実態には詳しい。コードの中身より「何をするか」の説明を重視する。GUIやSQLの操作は基本的に自分で行う(スクリーンショットで状況を共有するスタイル)。Claude Codeでは、より直接的にファイルを見て操作できるようになるはずなので、必要な確認はスクリーンショットに頼らず自分で行ってよい。
